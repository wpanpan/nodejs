// 读取图片

const fs = require('fs');
const path = require('path');

// fs.readFile(path.join(__dirname, './../img/google.png'), (error, data) => {
//   console.log(data.toString('base64'));
// });

var buffer = new Buffer('hello world', 'utf8');

